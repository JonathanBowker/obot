# Domain Caddy should serve, e.g. obot.example.com
# If you don't want to use your own domain, you can use:
#   <DROPLET_IP>.nip.io   or   <DROPLET_IP>.sslip.io
OBOT_DOMAIN=

# Tell Obot what its public URL host is (no scheme). Example: obot.example.com
OBOT_SERVER_HOSTNAME=

# Model provider keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional (helps avoid GitHub rate-limits when pulling tools/repos)
GITHUB_AUTH_TOKEN=

# Auth (recommended)
OBOT_SERVER_ENABLE_AUTHENTICATION=true
OBOT_BOOTSTRAP_TOKEN=
OBOT_SERVER_AUTH_OWNER_EMAILS=
OBOT_SERVER_AUTH_ADMIN_EMAILS=

# Usually leave as default
OBOT_SERVER_MCPRUNTIME_BACKEND=docker

# Optional: add HTTP basic auth at the proxy layer (Caddy).
# To enable it, copy `deploy/Caddyfile.basic-auth` over `deploy/Caddyfile` on the droplet,
# and set these two values.
OBOT_BASIC_AUTH_USER=
OBOT_BASIC_AUTH_HASH=
